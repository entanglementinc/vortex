#include <stdlib.h>
#include <stio.h>
#include <string.h>


typedef struct matrix
{
   int rows;             //number of rows.
   int cols;             //number of columns.
   unsigned short *data;
}*bin_matrix;
